version: v1.0
name: Docker
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu1804
blocks:
  - name: Build
    task:
      jobs:
        - name: docker build
          commands:
            - sudo apt update && wget https://raw.githubusercontent.com/golemfactory/golem/0.23.1/Installer/Installer_Linux/install.sh && chmod +x install.sh && ./install.sh
